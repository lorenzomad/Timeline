name: deploy to github pages

on:
  push:
    branches: gh-pages

jobs: 
  update-imports:
    name: update imports 
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    # replace the incorrect imports
    - run: sed -i -e 's+/assets+./assets+g' index.html
      
